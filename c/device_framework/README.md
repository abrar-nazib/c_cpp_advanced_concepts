# Struct EmbeddingPractice

This code demonstrates struct embedding in C. Struct embedding allows one struct to include another struct as a member, enabling the embedded struct's members to be accessed directly from the outer struct.